{
  "name": "express-proxy-platform",
  "version": "1.0.0",
  "private": true,
  "main": "index.js",
  "license": "MIT",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "start": "lerna run start --stream",
    "serve": "NODE_ENV=production yarn workspace server start",
    "build": "lerna run build"
  },
  "devDependencies": {
    "@lerna/batch-packages": "^3.16.0",
    "@lerna/filter-packages": "^3.18.0",
    "@lerna/project": "^3.21.0",
    "lerna": "^3.22.1"
  }
}
